# Day 3~5. 함수(FUNCTION) - 실습

> 아래 문제는 `01_실습데이터.md`의 `EMP` 테이블만으로 풀 수 있습니다.
> (교안에서 다룬 문자/숫자/날짜/형변환/NULL 처리 함수, CASE, 그룹 함수, GROUP BY/HAVING만 사용
> / MySQL 문법 기준)

---

## 기본

### 문제 1. 날짜 형식 변환
총무부(`'D9'`) 사원의 이름과 입사일을 `"2013년 03월 02일"`과 같은 형식의 문자열로 변환해서,
입사일이 빠른 순으로 조회하세요. (`DATE_FORMAT` 활용)

**Output**
```
곽상혁 2013년 03월 02일
권진우 2013년 09월 15일
김민혜 2014년 02월 10일
```

---

### 문제 2. 문자 길이 함수
회계관리부(`'D2'`) 사원의 이름, 이름의 글자 수(`CHAR_LENGTH`), 이름의 바이트 수(`LENGTH`)를
조회하세요.

**Output**
```
박홍주 3 9
심재호 3 9
엄용민 3 9
```

---

### 문제 8. LPAD 활용
기술지원부(`'D8'`) 사원의 이름과 사번(5자리, 왼쪽을 `'0'`으로 채움)을 사번 오름차순으로
조회하세요. (`LPAD` 활용)

**Output**
```
이광렬 00210
이금빈 00211
오미자 00212
```

---

### 문제 9. EXTRACT 활용
회계관리부(`'D2'`) 사원의 이름과 입사 연도(`EXTRACT(YEAR FROM HIRE_DATE)`)를 입사 연도가
빠른 순으로 조회하세요.

**Output**
```
박홍주 2015
심재호 2016
엄용민 2017
```

---

## 응용

### 문제 3. IFNULL + 산술 연산
해외영업1부(`'D5'`) 사원의 이름, 급여, 보너스 포함 연봉(`(급여 + 급여*IFNULL(보너스,0)) * 12`)을
연봉이 높은 사람부터 정렬해서 조회하세요.

**확인할 것**: 5행이 조회되고, 1위는 박지민(연봉 48,300,000), 꼴찌는 최주호(연봉 21,600,000)
입니다.

---

### 문제 4. CASE + GROUP BY
전 사원을 아래 기준으로 등급을 나누고, 등급별 인원수를 조회하세요.
- 급여 500만원 이상 → `'고액'`
- 급여 250만원 이상 500만원 미만 → `'중액'`
- 나머지 → `'소액'`

**Output**
```
고액 2
중액 11
소액 8
```

---

### 문제 5. GROUP BY + HAVING
부서별 평균 급여(1의 자리에서 반올림)를 구하되, 평균 급여가 300만원 이상인 부서만 부서코드와
평균 급여를 조회하세요.

**Output**
```
D6 3650000
D9 5900000
```
(순서는 무관합니다)

---

### 문제 10. SUBSTRING + INSTR
총무부(`'D9'`) 사원의 이름과 이메일 아이디(이메일에서 `'@'` 앞부분)를 사번 오름차순으로
조회하세요. (`INSTR`로 `'@'` 위치를 찾아 `SUBSTRING`에 활용)

**Output**
```
곽상혁 kwak_sh
권진우 kwon_jw
김민혜 kim_mh
```

---

### 문제 11. COALESCE 활용
회계관리부(`'D2'`)와 기술지원부(`'D8'`) 사원의 이름과 매니저 사번을 조회하되, 매니저가
없으면 `'없음'`으로 표시하세요(`COALESCE` 사용). 사번 오름차순으로 조회하세요.

**Output**
```
이광렬 200
이금빈 210
오미자 210
박홍주 없음
심재호 없음
엄용민 없음
```

---

### 문제 12. CASE + GROUP BY 종합
`JOB_CODE`를 기준으로 `'J1'`~`'J3'`은 `'고위직'`, `'J4'`~`'J5'`는 `'중간관리직'`,
`'J6'`~`'J7'`은 `'실무직'`으로 분류하고, 분류별 인원수와 평균 급여(1의 자리에서 반올림)를
조회하세요.

**Output**
```
고위직 6 4750000
중간관리직 6 2550000
실무직 9 2424027
```

---

## 도전

### 문제 6. 종합 - CASE로 성별 구분 후 집계
`EMP_NO`의 8번째 글자(`'1'`/`'3'`이면 `'남'`, `'2'`/`'4'`이면 `'여'`)로 성별을 구분해서,
성별별 인원수와 평균 급여(1의 자리에서 반올림)를 조회하세요.

**Output**
```
남 14 3106429
여 7  3160891
```

---

### 문제 7. 종합 - TIMESTAMPDIFF + WHERE
재직 중인(`ENT_YN = 'N'`) 사원 중, **2026-08-19**를 기준으로 재직 개월수가 120개월(10년)
이상인 사원의 이름, 입사일, 재직 개월수를 재직 개월수가 큰 순으로 조회하세요.
(`TIMESTAMPDIFF(MONTH, HIRE_DATE, '2026-08-19')` 활용, 현재 날짜 대신 고정된 날짜를 기준으로
계산해야 답이 항상 같게 나옵니다)

**확인할 것**: 7행이 조회되고, 1위는 곽상혁(161개월)입니다.

---

### 문제 13. 종합 - CASE로 출생연대 구분 후 집계
`EMP_NO`의 8번째 글자로 출생 세기를 판단해(`'1'`/`'2'`면 19XX년생, `'3'`/`'4'`면 20XX년생)
사원별 출생연도를 계산하고, 1980년대/1990년대/2000년대생으로 나눠 인원수를 조회하세요.

**Output**
```
1980년대 8
1990년대 10
2000년대 3
```

---

### 문제 14. 종합 - TIMESTAMPDIFF + CASE로 근속연수 구간별 집계
재직 중인(`ENT_YN = 'N'`) 사원을 **2026-08-19**를 기준으로 근속연수
(`TIMESTAMPDIFF(YEAR, HIRE_DATE, '2026-08-19')`)에 따라 `'5년미만'`, `'5~9년'`,
`'10년이상'`으로 구분하고, 구간별 인원수와 평균 급여(1의 자리에서 반올림)를 조회하세요.

**Output**
```
5년미만 4 2097500
5~9년 9 2610000
10년이상 7 4471429
```
