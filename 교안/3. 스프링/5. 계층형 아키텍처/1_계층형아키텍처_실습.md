# Day 5. 계층형 아키텍처 — 실습

교안의 `Emp` 계층 뼈대(`예제코드.md`)를 프로젝트에 옮겨 동작을 확인한 뒤, 같은 방식으로
**부서(Dept)** 계층을 직접 만듭니다. DB는 아직 없으므로 **메모리 구현**으로 합니다.

## 문제 1. Emp 계층 동작 확인

`예제코드.md` 의 8개 파일을 프로젝트에 만들고 실행한 뒤, 아래를 순서대로 호출해 결과를 적으세요.

```
POST /api/emps   {"empName":"신입","email":"a@b.com","deptId":5,"salary":2500000,"hireDate":"2026-09-01"}
GET  /api/emps
GET  /api/emps/201        (없는 id)
```

**확인할 것**: 마지막 요청의 HTTP 상태와 응답. 왜 그런지(Service의 `orElseThrow`) 설명.

## 문제 2. Dept 계층 만들기

Day 1 실습답안의 Dept 클래스 다이어그램을 참고해 아래를 만드세요.

- `domain/Dept` (deptId, deptName, location)
- `dto/DeptForm` (deptName, location)
- `repository/DeptRepository`(인터페이스) + `DeptMemoryRepository`(`@Repository`, 메모리)
- `service/DeptService`(인터페이스) + `DeptServiceImpl`(`@Service`)
- `controller/DeptController` (`@RestController`, `/api/depts`)
  - `GET /api/depts` 목록
  - `POST /api/depts` 등록 (본문 `DeptForm`)
  - `DELETE /api/depts/{id}` 삭제

## 문제 3. 계층 규칙 위반 찾기

아래 컨트롤러 코드에서 **계층 아키텍처 원칙 위반 2가지**를 찾아 고치세요.

```java
@RestController
@RequiredArgsConstructor
public class BadController {
    private final EmpRepository empRepository;

    @GetMapping("/bad/emps")
    public List<Emp> list() {
        List<Emp> all = empRepository.findAll();
        return all.stream()
                  .filter(e -> e.getSalary() >= 3_000_000)   // ← ?
                  .toList();
    }
}
```

## 문제 4. 구현 교체 실험

`EmpMemoryRepository` 옆에 `EmpFakeRepository` 를 하나 더 만들되(항상 사원 2명을 반환),
`@Repository` 대신 `@Repository @Primary` 를 붙이세요. 애플리케이션을 재실행하고 `GET /api/emps` 결과가
바뀌는지 확인하세요.

**질문**: `EmpServiceImpl` 이나 `EmpController` 를 한 줄이라도 고쳤나요? 왜 안 고쳐도 되었나요?

## 문제 5. (개념) 순환 참조

`EmpServiceImpl` 이 `DeptService` 를, `DeptServiceImpl` 이 `EmpService` 를 생성자 주입하도록 바꾸고
실행하면 어떤 에러가 나는지 확인하세요. 그리고 이 상황을 **설계로 푸는 방법**을 한 가지 제시하세요
(코드까지는 아니어도 됨).
