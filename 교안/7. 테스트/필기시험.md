# 자바 과정 최종 평가 - 필기시험

| 항목 | 내용 |
|---|---|
| 배점 | 필기 50점 + 실기 50점 = 100점 (문제 1은 10점, 문제 2~41은 문항당 1점) |
| 범위 | 전체 커리큘럼 (1. 기본 ~ 6. 파일입출력) |
| 문항 구성 | 전 문항 주관식(서술형/코드 작성형) 41문항 |

> 모든 문제는 서술형입니다. 핵심 키워드와 이유·원리가 포함되면 정답으로 인정합니다.

---

## 1과목. 자바 기본 문법 (문제 1~12)

**문제 1. (10점)** 다음은 자바의 예약어 30개입니다. 각 예약어가 어디에, 어떤 용도로 사용되는지 한 줄씩 간단히 서술하시오. (예약어 3개당 1점, 부분점수 인정)

> `class`, `interface`, `extends`, `implements`, `abstract`, `public`, `private`, `protected`, `static`, `final`, `void`, `return`, `new`, `this`, `super`, `package`, `import`, `if`, `else`, `switch`, `case`, `default`, `for`, `while`, `break`, `continue`, `try`, `catch`, `finally`, `throws`

---

**문제 2.** `int`, `double`, `boolean`, `String` 중 기본형(primitive type)이 아닌 것을 쓰고, 그 이유를 서술하시오.

---

**문제 3.** 다음 코드의 출력 결과가 `1`이 되도록 빈칸에 알맞은 연산자를 쓰시오.

```java
int a = 5;
int b = 2;
System.out.println(a ____ b); // 출력: 1
```

---

**문제 4.** `for`문과 `while`문을 각각 어떤 상황에 쓰는 것이 적합한지 서술하시오.

---

**문제 5.** 배열의 길이를 구하는 코드의 빈칸을 채우시오.

```java
int[] arr = {1, 2, 3};
System.out.println(arr.____); // 출력: 3
```

---

**문제 6.** 문자열의 내용이 같은지 비교할 때 `==` 대신 무엇을 사용해야 하며, 그 이유는 무엇인지 서술하시오.

---

**문제 7.** 배열을 메서드의 매개변수로 전달했을 때, 메서드 안에서 배열 원소를 변경하면 메서드 밖에서도 그 변경이 반영되는 이유를 서술하시오.

---

**문제 8.** 반환형이 있는 메서드를 작성할 때, 메서드 안에 반드시 포함되어야 하는 문법 요소는 무엇인지 쓰시오.

---

**문제 9.** 매개변수 타입이 다른 오버로딩이 되도록 빈칸을 채우시오.

```java
public class Calc {
    public int add(int a, int b) { return a + b; }
    public ____ add(double a, double b) { return a + b; }
}
```

---

**문제 10.** 생성자의 특징을 아는 대로 서술하시오. (이름 규칙, 반환형 여부, 오버로딩 가능 여부를 포함할 것)

---

**문제 11.** 매개변수와 필드 이름이 같을 때, 필드를 가리키기 위해 쓰는 키워드를 빈칸에 쓰시오.

```java
public class Box {
    private int value;
    public Box setValue(int value) {
        ____.value = value;
        return this;
    }
}
```

---

**문제 12.** 필드를 `private`으로 선언하고 `getter`/`setter`를 제공하는 이유를 서술하시오.

---

## 2과목. 객체지향 (문제 13~24)

**문제 13.** 자바에서 클래스는 몇 개까지 상속받을 수 있는지 쓰고, 관련된 특징을 서술하시오.

---

**문제 14.** 부모 클래스의 생성자를 호출하는 키워드를 빈칸에 쓰시오.

```java
public class Student extends Person {
    public Student(String name) {
        ____(name);
    }
}
```

---

**문제 15.** 오버라이딩(overriding)과 오버로딩(overloading)의 차이를 서술하시오.

---

**문제 16.** 추상 클래스가 일반 클래스와 다른 점, 그리고 인터페이스와 다른 점을 각각 서술하시오.

---

**문제 17.** 인터페이스를 구현할 때 쓰는 키워드를 빈칸에 쓰시오.

```java
public interface Printable {
    void printInfo();
}

public class Student ____ Printable {
    // ...
}
```

---

**문제 18.** `equals()`를 재정의할 때 반드시 함께 재정의해야 하는 메서드는 무엇이며, 그 이유는 무엇인지 서술하시오.

---

**문제 19.** 객체를 문자열로 표현할 때 재정의하는 `Object`의 메서드명을 빈칸에 쓰시오.

```java
@Override
public String ____() {
    return "Student{" + name + "}";
}
```

---

**문제 20.** 제네릭(Generic)을 사용하는 이유를 서술하시오.

---

**문제 21.** 제네릭에서 `int` 같은 기본형을 바로 사용할 수 없는 이유와, 대신 사용해야 하는 것이 무엇인지 서술하시오.

---

**문제 22.** `List` 인터페이스의 대표적인 구현체(배열 기반)를 빈칸에 쓰시오.

```java
List<String> list = new ____<>();
```

---

**문제 23.** 중복을 허용하지 않고 순서를 보장하지 않는 컬렉션 인터페이스의 이름을 쓰시오.

---

**문제 24.** `Map`에서 `key`와 `value`의 관계를 서술하시오.

---

## 3과목. DB 연동 (문제 25~29)

**문제 25.** JDBC로 DB에 연결할 때 필요한 정보를 쓰시오.

---

**문제 26.** 자바 프로그램에서 MySQL 데이터베이스에 접근하려면 어떤 라이브러리(드라이버)가 필요한지 쓰시오.

---

**문제 27.** 자원을 자동으로 닫아주는 문법의 키워드를 빈칸에 쓰시오.

```java
____ (Connection conn = DBUtil.getConnection()) {
    // ...
}
```

---

**문제 28.** 금액(급여 등)을 다룰 때 `double` 대신 `BigDecimal`을 쓰는 이유를 서술하시오.

---

**문제 29.** 다음은 페이지네비게이션에서 총 게시물 건수를 기준으로 마지막 페이지 번호를 구하는 코드입니다. 소수점 이하를 올림 처리하는 빈칸에 알맞은 메서드를 쓰시오.

```java
int size = 10;
int totalCnt = 99;
int realEndPageNo = (int) Math.____(totalCnt / (double) size);
```

---

## 4과목. 애노테이션 / 리플렉션 / DI (문제 30~32)

**문제 30.** 애노테이션이 실제로 어떤 필드에 붙어 있는지 프로그램이 "읽어서" 활용하려면 어떤 기술이 필요한지 쓰시오.

---

**문제 31.** 애노테이션 정보를 런타임까지 유지하도록 지정하는 메타 애노테이션의 이름을 빈칸에 쓰시오.

```java
@____(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
public @interface 필수입력 {
}
```

---

**문제 32.** DI(의존성 주입, Dependency Injection)가 무엇인지 서술하시오.

---

## 5과목. 디자인 패턴 / 빌더 패턴 (문제 33~35)

**문제 33.** 빌더 패턴을 쓰는 주된 이유를 서술하시오.

---

**문제 34.** 빌더 객체를 얻는 정적 메서드 이름을 빈칸에 쓰시오.

```java
Employee emp = Employee.____()
        .empName("홍길동")
        .build();
```

---

**문제 35.** GoF 디자인 패턴은 크게 세 가지로 분류됩니다. 그 세 가지를 쓰시오.

---

## 6과목. 람다식 (문제 36~38)

**문제 36.** 함수형 인터페이스가 되기 위한 조건을 서술하시오.

---

**문제 37.** `getter`/`setter`, 생성자, `toString()` 같은 반복적인 코드를 애노테이션만으로 자동 생성해주는 대표적인 라이브러리 이름을 쓰시오.

---

**문제 38.** `student -> student.getScore()`를 메서드 참조로 바꾸면 어떻게 되는지 쓰시오.

---

## 7과목. 파일 입출력 (문제 39~41)

**문제 39.** 텍스트 파일을 다룰 때 사용하는 스트림 계열(클래스 이름)을 쓰고, 바이트 스트림과 어떻게 다른지 서술하시오.

---

**문제 40.** `BufferedWriter`를 쓰면 왜 더 빠른지 서술하시오.

---

**문제 41.** 여러 사용자가 같은 이름의 파일을 업로드해도 서로 덮어쓰지 않게 하는 방법을 서술하시오.

---

수고하셨습니다. 답안지를 제출하세요.
